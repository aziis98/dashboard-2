{
    "name": "dashboard",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:extension": "npm run build && mkdir -p extension.local && cp -r dist/* extension.local && cp chrome-extension/* extension.local/"
    },
    "author": "aziis98",
    "license": "MIT",
    "devDependencies": {
        "@preact/preset-vite": "^2.5.0",
        "sass": "^1.58.0",
        "vite": "^4.1.1"
    },
    "dependencies": {
        "@fontsource/open-sans": "^4.5.14",
        "preact": "^10.11.3"
    }
}
